<?xml version="1.0" encoding="UTF-8"?>

<persistence
        xmlns="https://jakarta.ee/xml/ns/persistence"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            https://jakarta.ee/xml/ns/persistence
            https://jakarta.ee/xml/ns/persistence/persistence_3_1.xsd"
        version="3.1">

    <persistence-unit name="RecuperatorioPU" transaction-type="RESOURCE_LOCAL">

        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Entidades -->
        <class>ar.edu.utnfrc.k5a.recuperatorio.entities.Tarjeta</class>
        <class>ar.edu.utnfrc.k5a.recuperatorio.entities.Consumo</class>
        <class>ar.edu.utnfrc.k5a.recuperatorio.entities.Moneda</class>
        <class>ar.edu.utnfrc.k5a.recuperatorio.entities.Liquidacion</class>

        <properties>

            <!-- ===================================================== -->
            <!-- Configuración JDBC                                    -->
            <!-- ===================================================== -->

            <property name="jakarta.persistence.jdbc.driver"
                      value="org.h2.Driver"/>

            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:h2:mem:recuperatorio;DB_CLOSE_DELAY=-1"/>

            <property name="jakarta.persistence.jdbc.user"
                      value="sa"/>

            <property name="jakarta.persistence.jdbc.password"
                      value=""/>

            <!-- ===================================================== -->
            <!-- Hibernate                                             -->
            <!-- ===================================================== -->

            <property name="hibernate.dialect"
                      value="org.hibernate.dialect.H2Dialect"/>

            <property name="hibernate.show_sql"
                      value="true"/>

            <property name="hibernate.format_sql"
                      value="true"/>

            <property name="hibernate.highlight_sql"
                      value="true"/>

            <property name="hibernate.use_sql_comments"
                      value="true"/>

            <!-- ===================================================== -->
            <!-- Creación automática desde scripts                     -->
            <!-- ===================================================== -->

            <property name="jakarta.persistence.schema-generation.database.action"
                      value="drop-and-create"/>

            <property name="jakarta.persistence.schema-generation.create-source"
                      value="script"/>

            <property name="jakarta.persistence.schema-generation.create-script-source"
                      value="schema.sql"/>

            <property name="jakarta.persistence.sql-load-script-source"
                      value="data.sql"/>

        </properties>

    </persistence-unit>

</persistence>